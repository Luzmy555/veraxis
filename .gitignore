# Respaldos locales generados por scripts/backupUploads.js — no deben subirse al repo
montas-backend/backups/

# Logs generados por winston (logger.js) — no deben subirse al repo
montas-backend/logs/

# Variables de entorno con credenciales reales — nunca deben subirse al repo
montas-backend/.env

# Dependencias: se reinstalan con "npm install", no deben vivir en el repo
node_modules/

# Configuración local del editor/herramientas de desarrollo, no es parte del proyecto
.claude/
.vscode/
.vs/
