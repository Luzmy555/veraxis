// Único lugar donde vive la URL base del backend. Al desplegar a un dominio real,
// solo hay que cambiar esta línea (en vez de editar cada HTML por separado).
const API_BASE = 'http://localhost:4000';
